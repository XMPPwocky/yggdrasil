A control flow graph is a [[Graph (computer science)]] of a program's [[Control flow]], whose nodes are [[Basic block|basic blocks]] and whose edges are possible control flow transfers (e.g. jumps).

