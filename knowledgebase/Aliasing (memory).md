Roughly, when two [[Pointer]]s refer to the same actual memory address.

Alternatively but similarly, when two [[Virtual memory|virtual addresses]] are mapped to the same [[Physical memory|physical address]].