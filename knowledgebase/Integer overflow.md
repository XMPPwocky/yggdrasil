`calloc` is somewhat interesting as an attempt at creating a somewhat [[Misuse-resistant API]] to help with the "allocate an array of N objects of size M" situation, though it also zeroes the memory so...